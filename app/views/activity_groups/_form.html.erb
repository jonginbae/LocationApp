<%= form_for(activity_group) do |f| %>
  <% if activity_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(activity_group.errors.count, "error") %> prohibited this activity_group from being saved:</h2>

      <ul>
      <% activity_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :activity_id %>
    <%= f.number_field :activity_id %>
  </div>

  <div class="field">
    <%= f.label :student_id %>
    <%= f.number_field :student_id %>
  </div>

  <div class="field">
    <%= f.label :confirm %>
    <%= f.text_field :confirm %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
